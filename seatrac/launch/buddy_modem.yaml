# Core configuration
comms_plugin: "labust::seatrac::SeatracSim"
controller_plugin: "labust::seatrac::USBLController"
listener_plugins: ["labust::seatrac::StatusListener",
                   "labust::seatrac::DataListener",
                   "labust::seatrac::NavListener"]

# USBLController configuration
auto_mode: true
timeout: 10
transponders: [1,2]
transponder_names: ["surface","diver"]

# Simulation parameters
sim_node_id: 3
sim_is_modem: false

